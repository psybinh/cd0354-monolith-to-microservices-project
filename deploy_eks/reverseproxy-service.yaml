apiVersion: v1
kind: Service
metadata:
  name: reverseproxy
  labels:
    service: reverseproxy
spec:
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  selector:
    service: reverseproxy